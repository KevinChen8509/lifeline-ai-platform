package com.lifeline.modules.ai_model.entity.enums;

public enum FailureReason {
    device_offline,
    download_failed,
    checksum_mismatch,
    install_failed,
    timeout,
    device_not_compatible,
    binding_not_found
}
