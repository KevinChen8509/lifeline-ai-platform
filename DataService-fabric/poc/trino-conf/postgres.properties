# Trino PostgreSQL Catalog - 源 C 外部数据（风险标签）
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/external?ApplicationName=trino-fabric-poc
connection-user=external
connection-password=external123

# 性能与并发
metadata.cache-ttl=1h
metadata.cache-missing=true
