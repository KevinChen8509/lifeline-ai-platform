<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.0</version>
        <relativePath/>
    </parent>

    <groupId>com.datafabric</groupId>
    <artifactId>data-service</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <name>Data Fabric · Data Service</name>
    <description>Week 2 数据服务网关 - 调用 Cube.dev 语义层</description>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- Week 2 W2.3: AOP 嵌入式治理（脱敏 / 审计 / 血缘） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>
        <!-- B2: Spring Security 鉴权（PoC 用单一共享 API Key，生产换 Keycloak） -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- W3+F5: LangChain4j 1.18.1（1.0.0 → 1.18.1）
             - 1.0.0 的 OpenAI 流式 Delta 不认 GLM-4.7 的 reasoning_content（Jackson 解析失败）
             - 1.x 后续版本补齐 reasoning 模型支持；1.x 承诺 API 稳定
             不用 langchain4j-spring-boot-starter（仍 beta），改用手写 LangChainConfig -->
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j</artifactId>
            <version>1.18.1</version>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-open-ai</artifactId>
            <version>1.18.1</version>
        </dependency>

        <!-- W3 RawDbAgent: 直连 JDBC（对照组，刻意绕开治理） -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>com.clickhouse</groupId>
            <artifactId>clickhouse-jdbc</artifactId>
            <version>0.6.5</version>
            <classifier>all</classifier>
            <scope>runtime</scope>
        </dependency>
        <!-- F4 踩坑修复：all 分包被 minimizeJar 裁掉了 ClickHouseClient 接口，但
             ClickHouseDriver.<clinit> 直接引用它 → 驱动"注册成功但初始化失败"，
             DriverManager 队列里排在它之后的任何连接（含 B 路 CH 工具线上查询）全炸。
             显式补接口所在包（重复类由 classpath 顺序遮蔽，无冲突）。 -->
        <dependency>
            <groupId>com.clickhouse</groupId>
            <artifactId>clickhouse-client</artifactId>
            <version>0.6.5</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <!-- F8 B 路连接池（版本由 Boot BOM 管理）；监控端点需要读取池状态，不能 runtime -->
        <dependency>
            <groupId>com.zaxxer</groupId>
            <artifactId>HikariCP</artifactId>
        </dependency>
        <!-- F4 红队注入对比测试：H2 MySQL 兼容模式替身（无需起真源 DB） -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- W2.4 Pact 契约测试：consumer (data-service) ↔ provider (Cube.dev) -->
        <dependency>
            <groupId>au.com.dius.pact.consumer</groupId>
            <artifactId>junit5</artifactId>
            <version>4.6.16</version>
            <scope>test</scope>
        </dependency>
        <!-- F10 Pact provider 验证：consumer (dashboard) ↔ provider (data-service)
             注意 junit5（不带 spring）不含 MockMvcTestTarget，必须用 junit5spring -->
        <dependency>
            <groupId>au.com.dius.pact.provider</groupId>
            <artifactId>junit5spring</artifactId>
            <version>4.6.16</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <image>
                        <name>datafabric/data-service:${project.version}</name>
                    </image>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <!-- F10: Windows 默认 GBK 会把 pact JSON（UTF-8 中文契约）读花，
                         测试 JVM 强制 UTF-8（JDK 17 尚未默认 UTF-8，JEP 400 是 18+） -->
                    <argLine>-Dfile.encoding=UTF-8</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
